请为"小区物业管理系统"的【业主管理模块】生成完整后端代码。

要求按SSM分层架构生成以下文件：

1. Owner.java（实体类，对应owner表的所有字段，使用驼峰命名）
2. OwnerMapper.java（接口，包含增删改查方法）
3. OwnerMapper.xml（MyBatis映射文件，实现以下SQL：
   - 根据id查询
   - 模糊查询（按姓名和手机号）
   - 新增
   - 修改
   - 删除
   - 分页查询（使用limit）
4. OwnerService.java（接口）
5. OwnerServiceImpl.java（实现类，注入OwnerMapper，调用对应方法）
6. OwnerController.java（控制器，处理前端请求，包含：
   - 跳转到列表页
   - 返回JSON格式的分页数据
   - 新增/修改/删除接口，返回统一JSON格式 {code:0, msg:"操作成功"}）

所有类加上注释。Controller返回JSON数据使用@ResponseBody注解。