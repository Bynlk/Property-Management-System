<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.property.mapper.UserMapper">

    <select id="selectByUsername" resultType="com.property.entity.User">
        SELECT id, username, password, real_name AS realName, role, token_version AS tokenVersion
        FROM sys_user
        WHERE username = #{username}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, real_name, role, token_version, created_at, updated_at)
        VALUES (#{username}, #{password}, #{realName}, #{role}, #{tokenVersion}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updatePasswordAndIncrementVersion">
        UPDATE sys_user
        SET password = #{password}, token_version = token_version + 1, updated_at = NOW()
        WHERE username = #{username}
    </update>

    <update id="incrementTokenVersion">
        UPDATE sys_user
        SET token_version = token_version + 1, updated_at = NOW()
        WHERE username = #{username}
    </update>
</mapper>
