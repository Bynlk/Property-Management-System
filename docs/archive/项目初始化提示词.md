你是一个全栈工程师，请帮我搭建一个"小区物业管理系统"的Java Web项目骨架。

技术栈要求：
- 后端：Spring MVC + Spring + MyBatis（SSM框架）
- 前端：JSP + JSTL + Bootstrap（或Layui）
- 数据库：MySQL
- 构建工具：Maven
- 服务器：Tomcat

请按MVC分层结构创建项目目录，并生成以下核心配置文件：
1. pom.xml（包含所有依赖）
2. web.xml（配置Spring MVC前端控制器和字符编码过滤器）
3. spring-mvc.xml（配置注解驱动、视图解析器、静态资源放行）
4. spring-mybatis.xml（配置数据源、SqlSessionFactory、Mapper扫描）
5. jdbc.properties（数据库连接参数）
6. log4j.properties（日志配置）

数据库名为 property_management，字符集用utf8mb4。
请一次性生成所有文件，并说明每个文件的作用。