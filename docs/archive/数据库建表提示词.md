请为"小区物业管理系统"生成MySQL建表语句，数据库名为property_management。

要求包含以下表：

1. employee（员工表）：id、name、gender、phone、position（岗位）、hire_date（入职日期）
2. owner（业主表）：id、name、gender、phone、id_card（身份证号）、move_in_date（入住日期）
3. house（房屋表）：id、building（楼栋号）、unit（单元号）、room_number（房间号）、area（面积）、house_type（户型）、owner_id（关联业主，外键）、status（入住状态：已入住/空置/装修中）
4. fee（欠费表）：id、owner_id（关联业主）、house_id（关联房屋）、fee_type（费用类型：物业费/水费/电费/燃气费）、amount（金额）、should_pay_date（应缴日期）、status（缴费状态：已缴/未缴）
5. parking（停车位表）：id、spot_number（车位编号）、license_plate（车牌号）、owner_id（关联业主）、status（使用状态：使用中/空闲）
6. complaint（投诉表）：id、owner_id（关联业主）、title（投诉标题）、content（投诉内容）、create_time（投诉时间）、status（处理状态：待处理/处理中/已处理）
7. repair（报修表）：id、owner_id（关联业主）、device_name（设备名称）、fault_description（故障描述）、repair_person（维修人员）、status（维修状态：待维修/维修中/已完成）
8. duty（值班表）：id、employee_id（关联员工）、duty_date（值班日期）、shift（班次：早班/中班/晚班）

所有表必须包含主键id，使用自增。外键要明确标注。
请生成完整的SQL文件，可以直接在MySQL中执行。